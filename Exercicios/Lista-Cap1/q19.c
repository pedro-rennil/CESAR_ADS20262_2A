#include <stdio.h>
#include <stdlib.h>

int main(void) {
    // Emprega quebras de linha (\n) e tabs sequenciais (\t) dentro de um único printf
    printf("um\n\tdois\n\t\ttrês\n");

    system("PAUSE");
    return 0;
}