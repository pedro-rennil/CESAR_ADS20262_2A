## Questão 2

* A) Por que o uso de funções contidas em <conio.h> deve ser evitado em sistemas modernos 
(Linux, macOS, servidores)?

> Elas não fazem parte do padrão ANSI C / ISO C, sendo incompatíveis e não-portáveis em sistemas operacionais modernos

* B) Quais são as funções equivalentes e portáveis fornecidas pela biblioteca padrão <stdio.h> 
para entrada e saída de caracteres?

> * Entrada: `getchar()`, `fgetc(stdin)`. 
>* Saída: `putchar()`, `fputc(stdout)`


* C) 

~~~ 
#include <stdio.h>

int main (){

    char c;
    printf("Digite um caractere: ");
    scanf (" %c", &c);
    printf("Caractere lido com sucesso: %c \n", c);
    return 0;
}
~~~