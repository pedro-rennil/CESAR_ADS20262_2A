#include <stdio.h>
#include <stdlib.h>//não precisa do ;
int main(){ //main minusculo
    printf("existem %d semanas no ano",52);// entre aspas o conteudo exibido
    //cout << endl; não existe em C
    system("PAUSE");
    return 0;
}